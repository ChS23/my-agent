services:
  agent:
    build: .
    container_name: ai-agent
    restart: unless-stopped
    env_file: .env
    environment:
      - RUST_LOG=info,agent=debug
      - TZ=Europe/Moscow
    volumes:
      - agent-data:/app/data
      - ./skills:/app/skills

volumes:
  agent-data:
